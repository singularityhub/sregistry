apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-claim.ssl
spec:
  accessModes:
    - ReadOnlyMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 50Gi
  storageClassName: slow
  volumeName: minio.ssl
