apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: worker-claim.code
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 50Gi
  storageClassName: slow
  volumeName: worker.code
