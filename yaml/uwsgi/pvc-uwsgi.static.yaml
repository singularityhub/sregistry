apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uwsgi-claim.static
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 50Gi
  storageClassName: slow
  volumeName: uwsgi.static
